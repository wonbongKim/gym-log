# gym-log

개인 운동 루틴을 정의하고 운동을 기록하는 **모바일 우선 웹앱**.
혼자 쓰는 메모용 도구로, 서버 없이 브라우저 LocalStorage에만 데이터를 저장한다.

---

## 핵심 원칙

- **개인용 / 단일 사용자** — 로그인·인증·동기화 없음
- **서버리스** — 모든 데이터는 브라우저 LocalStorage에 저장
- **모바일 우선** — 운동 중 한 손으로 빠르게 입력
- **기록은 사실(fact)** — 루틴(템플릿)을 수정·삭제해도 과거 기록은 보존
- **백업은 사용자 책임** — JSON 내보내기/가져오기로 직접 백업

---

## 기술 스택

```
React + Vite
Tailwind CSS
LocalStorage (데이터 저장)
GitHub Pages (배포)
```

### 배포

```jsonc
// package.json
"scripts": {
  "deploy": "vite build && gh-pages -d dist"
}
```

```bash
npm install gh-pages --save-dev
npm run deploy   # vite build → gh-pages 브랜치로 자동 push
```

`vite.config`의 `base`는 GitHub Pages 저장소 경로(`/gym-log/`)에 맞춘다.

---

## 데이터 구조

### LocalStorage 키

| 키 | 내용 |
|---|---|
| `gymlog_exercises` | 운동 종목 마스터 |
| `gymlog_routines` | 루틴 정의 |
| `gymlog_logs` | 운동 기록 |
| `gymlog_session` | 진행 중인 운동 세션 (임시) |

각 키의 값은 최상위에 `version` 필드를 두어 향후 스키마 마이그레이션에 대비한다.

### 1) 운동 종목 마스터 (`gymlog_exercises`)

종목을 별도 마스터로 관리한다. 루틴과 기록은 종목을 `id`로 참조한다.
→ 오타 방지 + "벤치프레스 무게 추이" 같은 종목별 통계 집계가 가능해진다.

```jsonc
{
  "version": 1,
  "exercises": [
    {
      "id": "ex_uuid",
      "name": "벤치프레스",
      "bodyweight": false   // true면 맨몸 운동(보조무게 음수/0 허용)
    }
  ]
}
```

### 2) 루틴 정의 (`gymlog_routines`)

루틴은 **템플릿**이다. 종목은 마스터를 `exerciseId`로 참조한다.

```jsonc
{
  "version": 1,
  "routines": [
    {
      "id": "rt_uuid",
      "name": "월요일 - 가슴/등/삼두",
      "days": ["mon"],            // 월~일 멀티 선택
      "exercises": [
        {
          "exerciseId": "ex_uuid",  // 마스터 참조
          "targetSets": 3,
          "targetReps": "8~10",
          "order": 0                // 종목 순서
        }
      ]
    }
  ]
}
```

### 3) 운동 기록 (`gymlog_logs`)

기록은 **사실**이다. 루틴/종목이 나중에 바뀌거나 삭제돼도 기록은 그대로 남아야 하므로,
기록 시점의 **이름을 snapshot으로 함께 저장**한다(`routineName`, `exerciseName`).

```jsonc
{
  "version": 1,
  "logs": [
    {
      "id": "log_uuid",
      "date": "2026-06-26",
      "routineId": "rt_uuid",
      "routineName": "월요일 - 가슴/등/삼두",   // snapshot
      "startedAt": "2026-06-26T09:00:00+09:00",
      "finishedAt": "2026-06-26T10:05:00+09:00",
      "exercises": [
        {
          "exerciseId": "ex_uuid",
          "exerciseName": "벤치프레스",          // snapshot
          "sets": [
            { "setNo": 1, "kg": 80, "reps": 10 },
            { "setNo": 2, "kg": 80, "reps": 8 },
            { "setNo": 3, "kg": 75, "reps": 8 }
          ]
        }
      ]
    }
  ]
}
```

- `kg`는 `number | null` — 맨몸 운동은 `null`, 어시스트 머신은 음수 허용
- `routineId` / `exerciseId`는 참조 편의를 위해 유지하되, 표시는 항상 snapshot 이름 사용

### 4) 진행 중 세션 (`gymlog_session`)

운동 중 앱을 나갔다가 돌아와도 **이어서 기록**할 수 있도록, 세트를 입력할 때마다 즉시 저장한다.
"운동 종료" 시 이 세션을 `gymlog_logs`로 확정하고 세션을 비운다.

```jsonc
{
  "version": 1,
  "session": {
    "routineId": "rt_uuid",
    "routineName": "월요일 - 가슴/등/삼두",
    "startedAt": "2026-06-26T09:00:00+09:00",
    "exercises": [ /* logs.exercises와 동일 형태, 작성 중 상태 */ ]
  }
}
```

---

## 화면 구성

### 1. 홈

- 오늘 요일 기준 루틴 자동 표시 → "운동 시작"
- **루틴 없는 요일**: "오늘은 휴식일" 안내 + 다른 요일 루틴 바로 시작 가능
- 진행 중 세션이 있으면 "운동 이어하기" 우선 표시

### 2. 루틴 관리

- 루틴 목록 / 추가 / 수정 / 삭제
  - 루틴 이름, 요일 멀티 선택(월~일)
  - 종목 추가: 마스터에서 선택(없으면 즉석 생성) + 목표 세트/횟수
  - 종목 순서 변경(드래그 또는 ▲▼)
- 루틴 삭제 시 **과거 기록은 보존**됨을 명시

### 3. 운동 실행

- 종목별 세트 기록 (kg / reps)
- **직전 기록 표시**: 같은 종목의 가장 최근 기록을 "지난번 80kg×10"으로 보여주고,
  입력칸 **기본값으로 자동 채움** → 운동 중 입력 마찰 최소화
- 세트 추가 / 삭제 / 완료 체크
- 세트 입력 시마다 `gymlog_session`에 즉시 저장
- "운동 종료" → 기록 확정(`gymlog_logs`)

### 4. 기록 히스토리

- 날짜별 기록 목록 / 상세 보기
- (선택) 종목별 무게·횟수 추이
- **JSON 내보내기 / 가져오기** (수동 백업/복원)

---

## 데이터 보관 정책

- **자동 삭제 없음** — 기본적으로 모든 기록을 보존한다.
- **용량 한계 시에만 정리** — LocalStorage 쓰기가 quota 초과(`QuotaExceededError`)로
  실패할 때에 한해, 사용자에게 알리고 **오래된 기록부터** 정리하는 옵션을 제공한다.
  (예상 용량은 3개월 ~270KB 수준이라 평상시엔 발생하지 않음)
- 정리 전 **JSON 내보내기(백업)를 먼저 유도**한다.

---

## 예시 루틴 데이터

### 월요일 - 가슴/등 중심 + 삼두 (상체 A)
| 종목 | 세트 | 횟수 |
|---|---|---|
| 벤치프레스 | 3 | 8~10 |
| 어시스트 턱걸이 | 3 | 10~12 |
| 인클라인 벤치프레스 | 2 | 10~12 |
| 시티드 로우 | 2 | 10~12 |
| 사이드 레터럴 레이즈 | 3 | 15~20 |
| 케이블 푸시다운 | 3 | 12~15 |
| 덤벨 오버헤드 트라이셉스 익스텐션 | 3 | 12~15 |

### 수요일 - 하체 (무릎 보호 + 60분 볼륨)
| 종목 | 세트 | 횟수 |
|---|---|---|
| 레그프레스 | 4 | 12~15 |
| 레그 익스텐션 | 4 | 15~20 |
| 루마니안 데드리프트 | 4 | 10~12 |
| 레그컬 | 4 | 12~15 |
| 복근 운동 | 4 | 실패 지점까지 |

### 금요일 - 어깨/등 중심 + 이두 (상체 B)
| 종목 | 세트 | 횟수 |
|---|---|---|
| 숄더프레스 | 3 | 8~10 |
| 어시스트 턱걸이 | 3 | 10~12 |
| 벤치프레스 | 2 | 10~12 |
| 시티드 로우 | 2 | 10~12 |
| 사이드 레터럴 레이즈 | 3 | 15~20 |
| 해머컬 | 3 | 12~15 |
| 덤벨 바이셉스 컬 | 3 | 12~15 |

---

## 예상 용량

| 항목 | 크기 |
|---|---|
| 하루 운동 기록 1건 | ~2~3KB |
| 3개월(90일) | ~270KB |
| 루틴 · 종목 정의 | ~5KB |
| **총합** | **~300KB 이하** |

---

## 디자인 방향

30년차 UX 디자이너가 설계한 것처럼 **자연스럽고 모던하며 직관적인** 모바일 UI.

- 운동 중 한 손 엄지로 조작 가능한 큰 터치 타깃
- 직전 기록 기본값 채움으로 "입력"보다 "확인" 중심의 흐름
- 불필요한 화면 전환 최소화, 핵심 동선(시작→기록→종료) 3스텝 이내
```